<h1>Subir Archivo</h1>

<%= form_with url: create_for_reto_path(@reto.id), local: true, multipart: true do |form| %>
  <div>
    <%= form.label :file, "Selecciona un archivo (.txt) para subir" %>
    <%= form.file_field :file, accept: ".txt" %>
  </div>

  <div>
    <%= form.label :descripcion, "Descripción de la solución" %>
    <%= form.text_area :descripcion, placeholder: "Escribe una breve descripción de tu solución" %>
  </div>

  <div>
    <%= form.submit "Subir Archivo" %>
  </div>
<% end %>

<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>
<% if flash[:alert] %>
  <p><%= flash[:alert] %></p>
<% end %>